// This file is required by VS Code but doesn't need to do anything for a theme extension
export function activate() {
    // No activation needed for theme extension
}

export function deactivate() {
    // No deactivation needed for theme extension
}
